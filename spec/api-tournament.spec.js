const environment = require('./helpers/environment.js')
const superagent = require('superagent')

describe('api services for tournament managerment', () => {
  it('should be able to add a tournament', async () => {
    pending('finish later')
  })
})
